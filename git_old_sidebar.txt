tyle={{ width: '15px', height: '15px', color: '#EF4444' }} />
                    Sign Out
                  </button>
                </div>
              </div>
            )}
          </div>
        </div>
      </header>

      {/* CORE WORKSPACE LIMIT LAYOUT */}
      <div style={styles.workspace}>
        {/* LEFT SIDEBAR NAVIGATION */}
        <aside style={{ ...styles.sidebar, width: isSidebarCollapsed ? '72px' : '260px' }}>
          <div style={styles.sidebarScrollArea}>
            {menuGroups.map((group, groupIdx) => (
              <div key={group.group} style={styles.menuGroupContainer}>
                {!isSidebarCollapsed && <div style={styles.menuGroupTitle}>{group.group}</div>}
                <div style={{ display: 'flex', flexDirection: 'column', gap: '3px' }}>
                  {group.items.map(item => {
                    const isActive = activeTab === item.id || 
                      (item.id === 'settings' && activeTab === 'platform-control') ||
                      (item.id === 'reports' && activeTab === 'bi-reports') ||
                      (item.id === 'finance' && activeTab === 'finance-mgmt') ||
                      (item.id === 'employees' && activeTab === 'hr-mgmt');
                    
                    return (
                      <button
                        key={item.id}
                        onClick={() => { 
                          if (item.id === 'settings') {
                            setActiveTab('platform-control');
                            setCtrlSubTab('platform-dashboard');
                          } else if (item.id === 'reports') {
                            setActiveTab('bi-reports');
                            setBiSubTab('bi-dashboard');
                          } else if (item.id === 'finance') {
                            setActiveTab('finance-mgmt');
                            setFinSubTab('finance-dashboard');
                          } else if (item.id === 'customer-support') {
     